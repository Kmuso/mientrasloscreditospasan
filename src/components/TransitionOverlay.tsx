'use client'

/**
 * Overlay de transición cinemática.
 * Desactivado temporalmente — LoadingTransition maneja el overlay de entrada.
 * El archivo se mantiene para reactivar efectos de cortinilla (wipe/iris) en el futuro.
 */

export default function TransitionOverlay() {
  return null
}
